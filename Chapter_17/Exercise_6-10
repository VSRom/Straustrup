#include <iostream>
#include <vector>
#include <fstream>
#include <string>
//======================================================================================================================
void foo3()
{
	std::string *ptr7 = new std::string;
	std::cout << ptr7 << '\n';
	std::string *ptr9 = new std::string;
	std::cout << ptr9 << '\n';
	std::string *ptr10 = new std::string;
	std::cout << ptr10 << '\n';
	std::string *ptr11 = new std::string;
	std::cout << ptr11 << '\n';
	std::string *ptr12 = new std::string;
	std::cout << ptr12 << '\n' << '\n' << '\n';

}
//======================================================================================================================
void foo2()
{
	std::string pi1 = {};
	std::cout << &pi1 << '\n';
	std::string pi2 = {};
	std::cout << &pi2 << '\n';
	std::string pi3 = {};
	std::cout << &pi3 << '\n';
	std::string pi4 = {};
	std::cout << &pi4 << '\n';
}
//======================================================================================================================
int main()
{
	char symbol = {};

	std::string *ptr = new std::string;

	//	while (std::cin >> symbol)
	//	{
	//		if (symbol == '!') [[unlikely]]
	//			break;
	//	
	//		ptr->push_back(symbol);
	//	}
	//	
	//	for (int i = 0; i < ptr->size(); i++)
	//		std::cout << (*ptr)[i];

	foo2();
	foo3();

	return 0;
}
//======================================================================================================================
/*
//======================================================================================================================
	V	6. В этой главе ничего не говорилось о том, что произойдет, если, используя оператор new,
вы выйдете за пределы памяти.Это называется исчерпанием памяти(memory exhaustion).
Выясните, что случится.У вас есть две альтернативы : обратиться к документации
или написать программу с бесконечным циклом, в котором постоянно происходит
выделение памяти и никогда не выполняется ее освобождение.Попробуйте оба варианта.
Сколько памяти вы можете использовать, пока она не исчерпается ?
//======================================================================================================================
bool cicl = true;
	for (int i = 0; cicl; i++)
	{
		long double *ptr = new long double;	//64гб Завис ПК секунд за 30-50
	}
//======================================================================================================================
	V	7. Напишите программу, считывающую символы из потока cin в массив, расположенный в свободной памяти.
Читайте отдельные символы, пока не будет введен знак восклицания (!). Не используйте класс std::string.
Не беспокойтесь об исчерпании памяти.
//======================================================================================================================
char stroke = {};

	std::vector<char> *ptr = new std::vector<char>;

	while (std::cin >> stroke)
	{
		if (stroke == '!') [[unlikely]]
			break;

		ptr->push_back(stroke);
	}

	for (int i = 0; i < ptr->size(); i++)
		std::cout << (*ptr)[i];

	ptr->clear();
	delete ptr;
//======================================================================================================================
	V	8. Выполните упр. 7 еще раз, но теперь считывайте символы в строку std::string,
а не в свободную память (класс string знает, как использовать свободную память).
//======================================================================================================================
char symbol = {};

	std::string *ptr = new std::string;

	while (std::cin >> symbol)
	{
		if (symbol == '!') [[unlikely]]
			break;

		ptr->push_back(symbol);
	}

	for (int i = 0; i < ptr->size(); i++)
		std::cout << (*ptr)[i];

	ptr->clear();
	delete ptr;

	return 0;
//======================================================================================================================
	V	9. Как увеличивается стек: вверх (в сторону старших адресов) или вниз (в сторону младших адресов)?
В каком направлении возрастает занятая память изначально (т.е. пока вы не выполнили оператор delete)?
Напишите программу, позволяющую выяснить это.
//======================================================================================================================
void foo3()
{
	std::string *ptr7 = new std::string;
	std::cout << ptr7 << '\n';
	std::string *ptr9 = new std::string;
	std::cout << ptr9 << '\n';
	std::string *ptr10 = new std::string;
	std::cout << ptr10 << '\n';
	std::string *ptr11 = new std::string;
	std::cout << ptr11 << '\n';
	std::string *ptr12 = new std::string;
	std::cout << ptr12 << '\n' << '\n' << '\n';

}
//======================================================================================================================
void foo2()
{
	std::string pi1 = {};
	std::cout << &pi1 << '\n';
	std::string pi2 = {};
	std::cout << &pi2 << '\n';
	std::string pi3 = {};
	std::cout << &pi3 << '\n';
	std::string pi4 = {};
	std::cout << &pi4 << '\n';
}
//======================================================================================================================
	V	10. Посмотрите на решение упр. 7. Может ли ввод вызвать переполнение массива; иначе говоря, можете ли вы ввести больше
символов, чем выделено памяти (это серьезная ошибка)? Что произойдет, если вы введете больше символов, чем выделено памяти?
ОТВЕТ:
Это слишком много символов... А так неминуемо зависнет система или в лучшем случае случится крах программы
//======================================================================================================================
*/
