#include <iostream>
#include <fstream>
#include <vector>
//======================================================================================================================
void print_array10(std::ostream &os, int *a, int &n)
{
	for (int i = 0; i < n; i++)
		std::cout << "[" << a << "] == " << a[i] << '\n';
}
//======================================================================================================================
int main()
{
	std::vector<int> array;

	int size = 10;
	int j = 1;

	for (int i = 0; i < size; i++)
	{
		array.push_back(j);
		j *= 2;
	}
	int *p1 = array.data();		//C++11

	std::vector<int> array2;

	int *p2 = &array2[0];

}
/*
//======================================================================================================================
	V	1. Разместите в свободной памяти переменную типа int, инициализируйте ее число 7 и присвойте ее адрес указателю p1.

	V	2. Выведите на печать значения указателя p1 и переменной типа int, на которую он ссылается.
//======================================================================================================================
	int *p1 = new int;
	*p1 = 7;
	print_array10(std::cout, p1);
//======================================================================================================================
	V	3. Разместите в свободной памяти массив, состоящий из семи чисел типа int; инициализируйте его числами 1, 2, 4, 8 и т.д.; присвойте адрес массива указателю p2.

	V	4. Выведите на печать значение указателя p2 и массив, на который он ссылается.

	V	5. Объявите указатель типа int *с именем p3 и инициализируйте его значением указателя p2.

	V	6. Присвойте указатель p1 указателю p2.

	V	7. Присвойте указатель p3 указателю p2.

	V	8. Выведите на печать значения указателей p1 и p2, а также то, на что они ссылаются.

	V	9. Освободите всю память, которую использовали.
//======================================================================================================================
{
	int *p1 = new int;
	*p1 = 7;

	int size = 7;
	int *p2 = new int[size];
	int j = 1;

	for (int i = 0; i < size; i++)
	{
		p2[i] = j;
		j *= 2;
	}
	int *p3 = p2;
	p1 = p2;
	p2 = p3;

	std::cout << '\n' << "p1" << '\n';
	print_array10(std::cout, p1, size);

	std::cout << '\n' << "p2" << '\n';
	print_array10(std::cout, p2, size);

	delete[] p2;
}
//======================================================================================================================
	V	10. Разместите в свободной памяти массив, состоящий из десяти чисел типа int; инициализируйте их числами 1, 2, 4, 8 и т.д.; присвойте его адрес указателю p1.

	V	11. Разместите в свободной памяти массив, состоящий из десяти чисел типа int, присвойте его адрес указателю p2.

	V	12. Скопируйте значения из массива, на который ссылается указатель p1, в массив, на который ссылается указатель p2.
//======================================================================================================================
	int size = 10;
	int *array = new int[size];
	int j = 1;

	for (int i = 0; i < size; i++)
	{
		array[i] = j;
		j *= 2;
	}
	int *p1 = array;

	int *array2 = new int[size];

	for (int i = 0; i < size; i++)
	{
		array2[i] = j;
		j *= 2;
	}
	int *p2 = array;

	std::cout << '\n' << "p1" << '\n';
	print_array10(std::cout, p1, size);

	std::cout << '\n' << "p2" << '\n';
	print_array10(std::cout, p2, size);

	delete[] array, array2;
//======================================================================================================================
	V	13. Повторите задания 10–12, используя класс vector, а не массив.
//======================================================================================================================
std::vector<int> array;

	int size = 10;
	int j = 1;

	for (int i = 0; i < size; i++)
	{
		array.push_back(j);
		j *= 2;
	}
	int *p1 = array.data();		//C++11

	std::vector<int> array2;

	int *p2 = &array2[0];
//======================================================================================================================
