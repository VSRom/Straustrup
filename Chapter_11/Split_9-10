//		V	9. Напишите функцию vector<string> split(const string & s), возвращающую вектор подстрок аргумента s, разделенных пробелами.
//===============================================================================================================================
//		V	10. Напишите функцию vector<string> split(const string& s, const string& w), возвращающую вектор подстрок аргумента s,
//          между которыми стоят разделители, при условии, что в качестве разделителя может использоваться как обычный пробел, так и символы из строки w.
//===============================================================================================================================
#include <iostream>
#include <vector>
#include <string>
//===============================================================================================================================
std::vector<std::string> split(const std::string &s, const std::string &w)
{
	std::vector<std::string> result;
	std::string res;
	bool spacing = false;
	std::vector<char> space;
	char orspace;

	for (char g : w)
	{
		space.push_back(g);
	}

	for (char c : s)
	{
		if (c != ' ')
		{
			for (int i = 0; i < space.size(); i++)
			{
				if (c == space[i])
					spacing = true;
			}

			if (spacing == false)
			{
				res += c;
				continue;
			}

			result.push_back(res);
			res = {};
			spacing = false;
		}
	}

	result.push_back(res);
	return result;
}
//===============================================================================================================================
std::vector<std::string> split(const std::string &s)
{
	std::vector<std::string> result;
	std::string res;

		for (char c : s)
		{
			if (c != ' ')
				res += c;
			else
			{
				result.push_back(res);
				res = {};
			}
		}

	return result;
}
//===============================================================================================================================
int main()
{
	split("Hello'my/good;bad", "/.;:'");
}
//===============================================================================================================================
